---
slug: rust-free
name: "rust-free"
version: "1.0.0"
displayName: "Rust避坑指南(免费版)"
summary: "规避所有权、借用、生命周期、字符串、错误处理、并发与内存的常见陷阱。(免费版)。编写地道Rust代码,规避所有权移动、借用检查器、生命周期推断、 UTF-8字符串、错误处理、线程安全与智能指"
summary_zh: "规避所有权、借用、生命周期、字符串、错误处理、并发与内存的常见陷阱。(免费版)。编写地道Rust代码,规避所有权移动、借用检查器、生命周期推断、 UTF-8字符串、错误处理、线程安全与智能指"
license: "MIT"
description: "Rust避坑指南免费版,编写地道Rust代码,规避所有权移动、借用检查器、生命周期推断、UTF-8字符串、错误处理、线程安全与智能指针的高频陷阱.覆盖常见编译错误解读与Cargo陷阱排查.适用于Rust初学者与中级开发者的代码审查与学习场景,帮助开发者理解编译器报错并编写符合Rust习惯的代码,提升编译通过率与代码质量."
  编写地道Rust代码,规避所有权移动、借用检查器、生命周期推断、
  UTF-8字符串、错误处理、线程安全与智能指针的高频陷阱。覆盖
  常见编译错误与Cargo包管理陷阱。适用于独立开发者、企业团队
  和自动化工作流场景.
tools:
  - read
  - exec
  - write
homepage: ""
tags:
  - 研发工具
  - rust
  - automation
  - productivity
  - developer-tools
  - 工具
  - 效率
  - 集成
  - string
  - 借用
category: "Automation"
pricing_tier: free
---
# Rust避坑指南(免费版)

编写地道Rust代码,规避所有权、借用、生命周期、字符串、错误处理、迭代器、并发与内存的高频陷阱,以及常见编译错误与Cargo陷阱.
## 输入格式

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| input | string | 是 | Rust避坑指南(免费版)处理的输入数据或指令 |
| options | object | 否 | 附加配置选项,如模式选择、格式偏好等 |
| callback_url | string | 否 | 异步处理完成后的回调通知URL |

## 核心能力

### 1. 所有权与借用
- 变量移动后使用:显式 `clone()` 或用 `&` 借用
- `for item in vec` 移动vec:改用 `&vec` 或 `.iter()` 借用
- `String` 移入函数:只读访问传 `&str`
- 所有权是Rust编译错误的领先大来源

### 2. 借用检查器
- 不能同时持有 `&mut` 和 `&`:重构结构或用内部可变性
- 返回局部引用失败:返回所有权值而非引用
- `&mut self` 的可变借用阻塞所有访问:拆分结构体或用 `RefCell`

### 3. 生命周期
- `'static` 表示"可以"永久存活,非"已经":`String` 是 'static capable
- 含引用的结构体需要 `<'a>`:`struct Foo<'a> { bar: &'a str }`
- 返回引用的函数必须绑定输入:`fn get<'a>(s: &'a str) -> &'a str`

### 4. 字符串UTF-8陷阱
- `s[0]` 不编译:用 `.chars().nth(0)` 或 `.bytes()`
- `.len()` 返回字节数非字符数:用 `.chars().count()` 获取字符数
- `s1 + &s2` 移动s1:用 `format!("{}{}", s1, s2)` 保留两者

### 输出格式

完成响应以Markdown格式返回,包含任务状态(成功/失败)、解析摘要和具体输出数据。失败时返回错误码和错误信息,便于定位问题。- 验证返回数据的完整性和格式正确性
- 参考`输出格式`的配置文档进行参数调优

## 快速开始

1. 确认运行环境满足依赖说明中的要求
2. 在AI Agent对话中调用本技能,提供必要的输入参数
3. 检查输出结果,根据需要进行后续处理

> 详细的输入输出格式请参考下方章节说明。

## 适用场景

| 场景 | 输入 | 输出 |
|:-----|:-----|:-----|
| 所有权错误诊断 | 编译错误信息 | 移动/借用修复方案 |
| 生命周期标注 | 含引用的结构体/函数 | `<'a>` 标注与绑定规则 |
| 错误处理改造 | `unwrap()` 生产代码 | `?` + `Result` 返回类型 |

不适用于:Rust生态框架选型、嵌入式Rust特殊约束、FFI深度绑定、过程宏开发.
## 使用流程

1. 识别错误类别:所有权、借用、生命周期、字符串、错误处理、迭代器、并发、内存
2. 按编译错误表匹配修复方案
3. 优先借用而非clone,优先 `?` 而非 `unwrap()`
4. 并发场景验证 `Send`/`Sync` trait bound

**结果验证**: 任务完成后,查看输出确认状态。成功时返回摘要和数据;失败时根据错误信息排查,参考恢复章节获取修复步骤.
## 示例

### 示例:所有权修复
```rust
// 问题:变量移动后使用
let s1 = String::from("hello");
let s2 = s1;          // s1 移动到 s2
println!("{}", s1);   // 编译错误: value moved here
// ...
// 修复1:clone
let s1 = String::from("hello");
let s2 = s1.clone();
println!("{}", s1);   // OK
// ...
// 修复2:借用
let s1 = String::from("hello");
let s2 = &s1;         // 借用
println!("{} {}", s1, s2);  // OK
```

## 错误处理

| 错误场景 | 原因 | 处理方式 |
|---:|---:|---:|
| `value moved here` 编译错误 | 变量移动后仍使用 | 显式 `clone()` 或改用 `&` 借用 |
| `cannot borrow as mutable` 编译错误 | 已有不可变借用时取可变借用 | 重构借用顺序,或用 `RefCell` 内部可变性 |
| `missing lifetime specifier` 编译错误 | 编译器无法推断引用生命周期 | 为结构体与函数添加 `<'a>` 标注并绑定输入输出 |
| `the trait bound Send is not satisfied` | `Rc`/`RefCell` 跨线程使用 | 改用 `Arc`/`Mutex` 满足 `Send`/`Sync` |

## 常见问题

### Q1: `String` 与 `&str` 如何选择?
A: 函数参数优先 `&str`(只读、可接受 `String` 与 `&str`);结构体字段与返回值用 `String`(拥有所有权);需要零拷贝高性能场景用 `Cow<'a, str>`。`String` 移入函数会转移所有权,只读访问传 `&str` 更高效.
### Q2: `?` 操作符的返回类型要求?
A: `?` 用于 `Result`/`Option`,要求函数返回类型也是 `Result`/`Option`。`main` 函数用 `?` 需声明 `fn main() -> Result<(), Box<dyn std::error::Error>>`。`?` 会自动转换错误类型(若实现了 `From`).
### Q3: `.iter()` 与 `.into_iter()` 有何区别?
A: `.iter()` 借用集合元素( `&T`),原集合保留;`.into_iter()` 移动元素( `T`),原集合被消费。只读遍历用 `.iter()`,需要所有权转移用 `.into_iter()`。`.iter_mut()` 提供可变借用 `&mut T`.
## 已知限制

- 需要LLM支持生成代码修复方案
- 编译错误修复依赖完整错误信息,模糊描述可能误判
- 并发安全审查需理解完整调用图,局部代码可能遗漏

## 依赖说明

### 运行环境
- **Agent平台**: 支持SKILL.md的任意AI Agent（Claude Code / Cursor / Codex / Gemini CLI等）
- **操作系统**: Windows / macOS / Linux

### 依赖项
| 依赖项 | 类型 | 是否必需 | 获取方式 |
|:---:|:---:|:---:|:---:|
| LLM API | API | 必需 | 由Agent内置LLM提供 |

### API Key 配置
本Skill无需额外API Key（LLM能力由Agent平台内置提供）

### 可用性分类
- **分类**: MD+EXEC（）

## 升级提示

本免费版提供基础功能。升级到完整版 rust 获取全部能力和高级特性.
---
## 边界条件与限制

### 输入限制
- **输入数据长度**：技能处理输入数据时，建议不超过5000个字符，以避免性能问题。
- **复杂度**：输入数据应避免过于复杂的逻辑结构，如多层嵌套的代码块或大量引用，以保持处理效率和准确性。
- **错误信息**：输入的编译错误信息应尽可能具体，避免模糊不清，以便技能能够准确识别错误类型。

### 性能边界
- **处理速度**：对于简单的错误诊断和修复，技能通常在几秒内返回结果。对于复杂或大量代码的情况，处理时间可能会增加。
- **资源消耗**：技能在处理过程中可能会消耗一定量的内存和CPU资源，特别是在处理大型项目时。

### 兼容性约束
- **Rust版本**：技能主要针对Rust 1.0及以上版本，对于旧版本Rust可能无法提供完全兼容的解决方案。
- **编译器**：技能依赖于Rust编译器提供的错误信息，因此需要确保Rust编译器版本与技能兼容。

### 代码修复限制
- **代码结构**：技能可能无法处理复杂的代码重构，特别是涉及跨文件或模块的代码修改。
- **错误复杂性**：对于某些复杂的编译错误，技能可能无法提供完善的修复方案，需要开发者进一步手动调整代码。

### 其他限制
- **异步处理**：技能目前不支持异步处理，所有操作均为同步执行。
- **外部依赖**：技能不依赖于外部库或工具，因此无需担心兼容性问题。

## 输出格式限制

### 格式规范
- **Markdown格式**：技能的输出格式必须遵循Markdown规范，以确保在不同平台和工具中正确显示。
- **结构化输出**：输出应包含明确的标题、段落和列表，以便用户快速理解和处理信息。

### 内容限制
- **信息完整性**：输出内容应包含所有必要的信息，包括错误类型、修复方案和进一步的操作指导。
- **简洁性**：输出内容应避免冗余和无关信息，确保用户能够快速找到所需信息。

### 显示限制
- **字符限制**：输出内容的长度应控制在合理的范围内，避免过长导致阅读困难。
- **代码块限制**：输出的代码块应保持整洁，避免因代码格式问题影响阅读。

### 异常处理
- **错误信息**：在处理过程中遇到异常时，技能应返回清晰的错误信息，以便用户了解问题原因。
- **恢复建议**：对于无法处理的错误，技能应提供可能的恢复步骤或建议，帮助用户解决问题。

## 使用场景限制

### 不适用场景
- **Rust生态框架选型**：技能不提供框架选型建议，开发者应根据具体需求选择合适的框架。
- **嵌入式Rust特殊约束**：技能不处理嵌入式Rust的特殊约束，如硬件资源限制等。
- **FFI深度绑定**：技能不涉及FFI（Foreign Function Interface）的深度绑定问题。
- **过程宏开发**：技能不提供过程宏开发相关的支持，开发者应使用专门的工具和库。

### 适用场景
- **日常开发**：技能适用于日常Rust代码编写，帮助开发者规避常见陷阱，提高代码质量。
- **代码审查**：技能可用于代码审查过程，辅助发现潜在问题。
- **自动化工作流**：技能可集成到自动化工作流中，提高开发效率和代码质量。

## 维护与更新

### 维护计划
- **定期更新**：技能将定期更新，以适应Rust语言和生态的变化。
- **用户反馈**：技能的开发者将关注用户反馈，根据用户需求改进功能。
- **性能优化**：技能将不断优化性能，提高处理速度和资源消耗。

### 更新内容
- **新增功能**：根据用户需求，技能将新增功能，如支持更多Rust语言特性。
- **修复bug**：技能将修复已知的bug，提高稳定性和可靠性。
- **文档更新**：技能的文档将同步更新，以反映最新的功能和用法。

### 用户支持
- **社区支持**：技能的开发者将积极参与社区讨论，提供技术支持。
- **官方支持**：技能的开发者将提供官方支持，包括技术支持和问题解答。

<!-- keyword-enriched -->
## 质量增强补充

### 可靠性增强(Reliability Enhancement)

已实现以下异常处理与可靠性保障:
- - 降级策略与默认值(fallback/default value)处理
- 重试机制(retry with backoff)

### 适用性增强(Adaptability Enhancement)

- - 触发条件(trigger)与激活方式
