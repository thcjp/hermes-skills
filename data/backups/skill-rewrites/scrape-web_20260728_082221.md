---
slug: scrape-web
name: scrape-web
version: "1.0.0"
displayName: Scrape Web
summary: 使用 Python + Scrapling 获取网页内容，支持简单选择器
license: MIT-0
description: |-
  使用 Python + Scrapling 获取网页内容，支持简单选择器

  核心能力:

  - 研究工具领域的专业化AI辅助工具

  - 基于高人气开源Skill深度优化升级

  - 移除风险代码,增强安全性和稳定性

  适用场景:

  - 数据研究、文献分析、信息收集

  - 独立开发者与一人公司效率提升

  - 自动化工作流与智能决策辅助

  差异化:经过深度优化,去除原始风险代码,清理外部依赖引用,增强元数据和触发关键词,完全适配SkillHub平台规范
tags:
- Research
tools:
  - - read
- exec
pricing_tier: "L2"
pricing_model: "per_use"
suggested_price: 19.9
---


# Scrape Web

使用 Scrapling 获取网页内容，返回纯文本或选择器结果。

## 依赖说明

```bash
pip install "scrapling[all]"
scrapling install
pip install httpx
```


## 依赖说明

### 依赖版本和兼容性

- **Python**：Python 3.6或更高版本。
- **scraping**：scraping库版本应与您的Python版本兼容。
- **httpx**：httpx库版本应与您的Python版本兼容。

请注意，依赖项的版本可能会随着时间而更新，请确保您使用的是最新版本的依赖项。

## 用法

### 1) 直接抓取纯文本

```bash
python scripts/scrape_web.py --url "https://example.com"
```

### 2) 使用 CSS 选择器

```bash
python scripts/scrape_web.py --url "https://example.com" --selector "title::text"
```

### 3) 保存到文件

```bash
python scripts/scrape_web.py --url "https://example.com" --out "output.txt"
```



## 代码示例

### 可运行的技术示例

```python
import scraping
from httpx import Client

client = Client()
response = client.get('https://example.com')
content = scraping.scrape(response.text, selector='title::text')
print(content)
```

## 参数描述

### 参数描述

- **--url**：指定要抓取的网页URL。这是必需的参数。
- **--selector**：指定要提取的CSS选择器。如果未指定，默认为整个网页的纯文本内容。
- **--out**：指定输出文件的路径。如果未指定，结果将打印到控制台。
- **--format**：指定输出格式，可以是'plain'（纯文本）或'json'（JSON格式）。默认为'plain'。

## 依赖说明

### 运行环境
- **Agent平台**: 支持SKILL.md的任意AI Agent(Claude Code / Cursor / Codex / Gemini CLI等)
- **操作系统**: Windows / macOS / Linux

### 第三方依赖
| 依赖项 | 类型 | 是否必需 | 获取方式 |
|:-------|:-----|:---------|:---------|
| LLM API | API | 必需 | 由Agent内置LLM提供 |

### API Key 配置
- 本Skill基于Markdown指令,无需额外API Key(除内容中明确标注的外部API)

### 可用性分类
- **分类**: MD+EXEC(纯Markdown指令,部分功能需要exec命令行执行能力)
- **说明**: 基于Markdown的AI Skill,通过自然语言指令驱动Agent执行任务

## 核心能力

- 触发关键词: web, 支持简单选择, python, 使用, scrape, 获取网页内容, scrapling

## 适用场景

| 场景 | 输入 | 输出 |
|------|------|------|
| 基础使用 | 用户请求 | 处理结果 |

**不适用于**：需要人工判断的复杂决策场景


## 使用场景示例

### 使用场景示例

- **数据研究**：使用Scrape Web抓取学术文章的标题和摘要，以便进行文献分析。
- **信息收集**：抓取新闻网站上的最新新闻标题和内容，以便进行实时信息监控。
- **自动化工作流**：将Scrape Web集成到自动化脚本中，以自动抓取特定网站的数据。

## 使用流程

1. 确认运行环境满足依赖说明中的要求
2. 根据适用场景选择合适的使用方式
3. 执行操作并检查输出结果
4. 如遇错误，参考错误处理章节

## 示例

### 示例1：基础用法

```
输入: 用户请求
处理: 根据使用流程执行
输出: 处理结果
```

## 错误处理

| 错误场景 | 原因 | 处理方式 |
|---------|------|---------|
| 配置错误 | 参数缺失或格式错误 | 检查依赖说明中的配置要求 |
| 运行时错误 | 运行环境不满足 | 确认运行环境符合依赖说明 |
| 网络错误 | 连接超时或不可达 | 检查网络连接后重试，参考国内替代方案 |



## 边界条件

### 边界条件

- **空URL**：如果提供的URL为空或无效，Scrape Web将返回一个错误消息。
- **无效选择器**：如果提供的CSS选择器无效，Scrape Web将无法提取数据，并返回一个错误消息。
- **无响应网站**：如果目标网站无响应或拒绝连接，Scrape Web将返回一个错误消息。
- **文件保存错误**：如果无法将结果保存到文件中，Scrape Web将返回一个错误消息。

## 错误处理

### 错误处理流程

1. **检查配置**：确保所有必需的参数都已正确设置，包括URL、选择器和其他配置选项。
2. **检查依赖**：确认所有依赖项都已正确安装，并且版本兼容。
3. **检查网络**：确保您的网络连接稳定，并且可以访问目标网站。
4. **查看日志**：如果出现错误，检查日志文件以获取更多详细信息。
5. **联系支持**：如果问题仍然存在，请联系技术支持以获取帮助。

## 常见问题

### Q1: 如何开始使用Scrape Web？
A: 请先阅读使用流程章节，确认环境满足依赖说明中的要求。

### Q2: 遇到错误怎么办？
A: 请参考错误处理章节，按照表格中的处理方式操作。

### Q3: Scrape Web有什么限制？
A: 请参考已知限制章节了解具体限制。


## FAQ

### Q1: 如何开始使用Scrape Web？
A: 请先阅读使用流程章节，确认环境满足依赖说明中的要求。

### Q2: 遇到配置错误怎么办？
A: 检查您的配置文件，确保所有必需的参数都已正确设置。如果问题仍然存在，请尝试重新安装依赖项。

### Q3: 如何处理运行时错误？
A: 确认您的运行环境满足依赖说明中的要求。如果问题仍然存在，请检查您的网络连接或尝试使用不同的代理服务器。

### Q4: 如何处理网络错误？
A: 检查您的网络连接是否稳定。如果问题仍然存在，请尝试重试操作或联系技术支持。

### Q5: Scrape Web支持哪些浏览器？
A: Scrape Web不直接支持任何浏览器。它通过Python的requests库来发送HTTP请求，因此理论上支持所有支持HTTP请求的浏览器。

## 已知限制

- 需要LLM支持，无LLM环境无法使用
- 复杂场景可能需要人工辅助判断
- 性能取决于底层模型能力


## 差异化优势


## 同类方案对比

### 同类方案对比

与市场上其他网页抓取工具相比，Scrape Web在安全性和稳定性方面具有明显优势。它还提供了更简洁的API和更丰富的使用场景，使得用户可以更轻松地实现网页内容的抓取和分析。

### 差异化优势

Scrape Web通过深度优化和去除原始风险代码，提供了更高的安全性和稳定性。它还通过增强元数据和触发关键词，完全适配SkillHub平台规范，确保了与平台的良好兼容性。
