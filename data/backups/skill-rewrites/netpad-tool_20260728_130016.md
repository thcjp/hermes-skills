---

slug: "netpad-tool"
name: "netpad-tool"
version: "1.0.0"
displayName: "工具"
summary: "NetPad管理工具,管表单/提交/用户(删除需谨慎)。The skill appears to be a legitimate NetPad management tool, but it"
summary_zh: "NetPad管理工具,管表单/提交/用户(删除需谨慎)。The skill appears to be a legitimate NetPad management tool, but it"
license: "MIT"
description: The skill appears to be a legitimate NetPad management tool, but it gives an agent powerful delet。可处理提升工作效率
tags:
  - Integrations
  - 工具
  - 效率
  - 写作
  - netpad
  - api
  - key
tools:
  - read
  - exec
  - write
homepage: ""
category: "Automation"

---

```markdown
# Netpad Tool

## 付费版专享能力

| 能力 | 免费版 | 付费版 |
|---|---|---|
| 基础功能 | 支持 | 支持 |
| NetPad实例管理 | 不支持 | 支持 |
| 复杂工作流可视化编排 | 不支持 | 支持 |
| 条件分支与异常重试 | 不支持 | 支持 |
| 定时触发与事件驱动 | 不支持 | 支持 |
| 执行日志与审计追踪 | 不支持 | 支持 |

## 核心能力

Netpad Tool是一款功能强大的NetPad管理工具，它能够帮助用户高效地管理NetPad实例、表单、提交和用户。特别需要注意的是，该工具提供了强大的删除功能，用户在使用时需格外谨慎。

### 功能概述

- **实例管理**：创建、删除、更新NetPad实例。
- **表单管理**：创建、编辑、删除表单。
- **提交管理**：查看、编辑、删除提交。
- **用户管理**：创建、编辑、删除用户。
- **日志审计**：查看操作日志，进行审计追踪。

## 快速开始

### 环境准备

1. **运行环境**：确保您的Agent平台（如Claude Code、Cursor、Codex、Gemini CLI等）支持SKILL.md。
2. **操作系统**：Windows、macOS或Linux。
3. **工具依赖**：LLM API，由Agent内置LLM提供。
4. **API Key配置**：使用以下命令配置API Key。

```bash
export API_KEY="your_api_key_here"
```

配置后需重启会话或开启新终端生效。API Key应妥善保管，避免泄露到版本控制系统。

### 使用步骤

1. 在AI Agent对话中调用Netpad Tool技能，提供必要的输入参数。
2. 检查输出结果，根据需要进行后续处理。

> 详细的输入输出格式请参考下方章节说明。

## 适用场景

Netpad Tool适用于以下场景：

- **NetPad实例管理**：创建、删除、更新NetPad实例。
- **表单管理**：创建、编辑、删除表单。
- **提交管理**：查看、编辑、删除提交。
- **用户管理**：创建、编辑、删除用户。

## 使用流程

1. 确认运行环境满足依赖说明中的要求。
2. 根据适用场景选择合适的使用方式。
3. 执行操作并检查输出结果。
4. 如遇错误，参考错误处理章节。

## 输入格式

| 参数名 | 类型 | 必填 | 说明 |
|---:|---:|---:|---:|
| target | string | 是 | 目标NetPad实例或主机地址 |
| action | string | 否 | 操作类型，可选：manage, delete, list, default: manage |
| form_id | string | 否 | 表单ID，仅当action为edit或delete时必填 |
| submit_id | string | 否 | 提交ID，仅当action为edit或delete时必填 |
| user_id | string | 否 | 用户ID，仅当action为edit或delete时必填 |

## 输出格式

```json
{
  "success": true,
  "data": {
    "overall_grade": "A",
    "total_score": 92,
    "max_score": 100,
    "summary": "处理完成",
    "details": [
      {
        "item": "代码风格",
        "status": "pass",
        "score": 95,
        "comment": "符合规范"
      },
      {
        "item": "安全合规",
        "status": "warn",
        "score": 80,
        "comment": "符合规范"
      }
    ],
    "improvements": [
      {
        "priority": "high",
        "suggestion": "建议优化",
        "expected_gain": "+5分"
      },
      {
        "priority": "medium",
        "suggestion": "建议优化",
        "expected_gain": "+3分"
      }
    ]
  },
  "error": null
}
```

## 错误处理

| 错误场景 | 原因 | 处理方式 |
|:------|------:|:------|
| LLM响应超时或无响应 | 网络延迟或模型负载过高 | 检查网络连接，请求重试；确认Agent平台LLM服务正常 |
| 输入内容格式不正确 | 用户输入不符合skill预期格式 | 检查输入是否符合skill使用说明中的格式要求，参考示例章节 |
| 执行结果与预期不符 | 指令描述不够明确或上下文不足 | 提供更详细的指令描述，补充必要的上下文信息 |
| 命令执行失败 | 运行环境不满足要求或权限不足 | 确认运行环境符合依赖说明中的要求；检查命令权限设置 |

## 安全注意事项

- 使用Netpad Tool时，请确保API Key安全，避免泄露。
- 在进行删除操作时，请确认目标对象无误，以免造成不可逆的损失。
- 定期检查操作日志，以便及时发现潜在的安全风险。

## 已知限制

- 需要API Key，无Key环境无法使用。

## 总结

Netpad Tool是一款功能强大的NetPad管理工具，它能够帮助用户高效地管理NetPad实例、表单、提交和用户。通过详细的功能描述、使用流程和错误处理，用户可以轻松上手并使用Netpad Tool。在安全使用的前提下，Netpad Tool将为用户带来极大的便利。