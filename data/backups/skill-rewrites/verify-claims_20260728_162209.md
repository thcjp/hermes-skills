---
slug: "verify-claims"
name: "verify-claims"
version: 1.0.1
displayName: "事实核查技能"
summary: "事实核查技能(会自动建延迟跟进需谨慎)。This fact-checking skill is mostly coherent, but it can automatically creat"
summary_zh: "事实核查技能(会自动建延迟跟进需谨慎)。This fact-checking skill is mostly coherent, but it can automatically creat"
license: "MIT"
description: |-
  This fact-checking skill is mostly coherent, but it can automatically
  create delayed follow-up ch。Use when 需要AI模型调用、智能对话、Agent编排、LLM应用时使用。不适用于需要100%确定性的关键决策.
tags:
  - Research
  - 工具
  - 效率
  - 示例数据
  - step
  - content
  - gate
tools:
  - read
  - exec
  - glob
  - grep
homepage: ""
category: "Automation"
---
# Verify Claims

## 付费版专享能力

| 能力 | 免费版 | 付费版 |
|---|---|---|
| 基础功能 | 支持 | 支持 |
| 复杂工作流可视化编排 | 不支持 | 支持 |
| 条件分支与异常重试 | 不支持 | 支持 |
| 定时触发与事件驱动 | 不支持 | 支持 |
| 执行日志与审计追踪 | 不支持 | 支持 |
| 分布式任务调度与负载均衡 | 不支持 | 支持 |

## 核心能力

- This fact-checking skill is mostly coherent, but it can automatically
  create delayed follow-up ch
#
## 快速开始

1. 确认运行环境满足依赖说明中的要求
2. 在AI Agent对话中调用本技能,提供必要的输入参数
3. 检查输出结果,根据需要进行后续处理

> 详细的输入输出格式请参考下方章节说明。

## 适用场景

| 场景 | 输入 | 输出 |
|:-----|:-----|:-----|
| 事实核查技能 | 目标数据与配置参数 | 处理结果与执行状态 |
| 会自动建延迟跟进需谨 | 目标数据与配置参数 | 处理结果与执行状态 |

**不适用于**：需要人工判断的复杂决策场景

## 使用流程

1. 确认运行环境满足依赖说明中的要求
2. 根据适用场景选择合适的使用方式
3. 执行操作并检查输出结果
4. 如遇错误，参考错误处理章节

## 输入格式

| 参数名 | 类型 | 必填 | 说明 |
|---:|---:|---:|---:|
| content | string | 否 | verify-claims处理的内容输入 |,  |
| mode | string | 否 | 处理模式, 可选: json/text/markdown,  |
| max_retries | integer | 否 | 单步最大重试次数, 默认: 2 |
| skip_steps | array | 否 | 跳过的步骤编号(用于断点续传), 默认: [] |

## 输出格式

```json
{
  "success": true,
  "data": {
    "final_result": {
      "claims_result": "claims_result_value",
      "claims_metadata": "claims_metadata_value",
      "claims_status": "claims_status_value"
    },
    "execution_log": [
      {
        "step": 1,
        "name": "按流程执行",
        "status": "completed",
        "duration_ms": 1200,
        "output_summary": "按流程执行"
      },
      {
        "step": 2,
        "name": "按流程执行",
        "status": "completed",
        "duration_ms": 3500,
        "output_summary": "按流程执行"
      },
      {
        "step": 3,
        "name": "按流程执行",
        "status": "completed",
        "duration_ms": 2100,
        "output_summary": "按流程执行"
      },
      {
        "step": 4,
        "name": "按流程执行",
        "status": "completed",
        "duration_ms": 800,
        "output_summary": "按流程执行"
      }
    ],
    "total_duration_ms": 7600,
    "gates_passed": 3,
    "gates_total": 3
  },
  "error": null
}
```

中间产物模板参考: `assets/verify-claims_template`

## 异常处理

| 错误场景 | 原因 | 处理方式 |
|:---:|:---:|:---:|
| 配置错误 | 参数缺失或格式错误 | 检查依赖说明中的配置要求 |
| 运行时错误 | 运行环境不满足 | 确认运行环境符合依赖说明 |
| 网络错误 | 连接超时或不可达 | 

## 依赖说明

### 运行环境
- **Agent平台**: 支持SKILL.md的任意AI Agent(Claude Code / Cursor / Codex / Gemini CLI等)
- **操作系统**: Windows / macOS / Linux

### 依赖说明(补充)
| 依赖项 | 类型 | 是否必需 | 获取方式 |
|:------|------:|:------|:------|
| LLM API | API | 必需 | 由Agent内置LLM提供 |

### API Key 配置
- 

### 可用性分类
- **分类**: MD+EXEC()
- **说明**: 基于Markdown的AI Skill,

**API Key配置方式**:
```bash
export API_KEY="your_api_key_here"
```
配置后需重启会话或开启新终端生效。API Key应妥善保管,避免泄露到版本控制系统.
## 案例展示

### 示例1: 基础用法
**输入**:
```json
{
  "content": "示例数据",
  "content": "示例数据",
  "mode": "示例数据"
}
```
**执行日志**:
```
Step 1 [按流程执行]: 示例数据 ✓ (1.2s)
  Gate: 示例数据 ✓
Step 2 [按流程执行]: 示例数据 ✓ (3.5s)
  Gate: 示例数据 ✓
Step 3 [按流程执行]: 示例数据 ✓ (2.1s)
  Gate: 示例数据 ✓
Step 4 [按流程执行]: 示例数据 ✓ (0.8s)
```
**最终输出**:
```
示例数据
```

### 示例2: 进阶用法
**输入**:
```json
{
  "content": "示例数据",
  "mode": "示例数据"
}
```
**执行日志**:
```
Step 1 [按流程执行]: 示例数据 ✓ (0.9s)
  Gate: 示例数据 ✓
Step 2 [按流程执行]: 示例数据 ✓ (2.8s)
  Gate: 示例数据 ✗ → 重试
  Gate: 示例数据 ✓
Step 3 [按流程执行]: 示例数据 ✓ (1.5s)
  Gate: 示例数据 ✓
```
**最终输出**:
```
示例数据
```

### 示例3: 边界情况 - 边界情况
**输入**:
```json
{
  "content": "示例数据",
  "max_retries": 1
}
```
**执行日志**:
```
  Gate: 示例数据 ✓
Step 2 [按流程执行]: 示例数据 ✗ → 重试(1/1)
Step 2 [按流程执行]: 示例数据 ✗ → 超过最大重试次数
流程暂停, 断点: Step 2
```
**输出**(部分结果):
```json
{
  "success": false,
  "error": "Step 2 failed after 1 retries",
  "data": {
    "completed_steps": [1],
    "checkpoint": "step_2",
    "partial_result": "示例数据"
  }
}
```

## 常见问题

### Q1: 如何开始使用Verify Claims？
A: 

## 错误处理

| 错误场景(续)| 原因 | 处理方式 |
|----:|:----|----:|
| LLM响应超时或无响应 | 网络延迟或模型负载过高 | ，请求；确认Agent平台LLM服务正常 |
| 输入内容格式不正确 | 用户输入不符合skill预期格式 | 检查输入是否符合skill使用说明中的格式要求，参考示例章节 |
| 执行结果与预期不符 | 指令描述不够明确或上下文不足 | 提供更详细的指令描述，补充必要的上下文信息 |
| 命令执行失败 | 运行环境不满足要求或权限不足 | 确认运行环境符合依赖说明中的要求；检查命令权限设置 |

## 已知限制

This fact-checking skill has the following known limitations. Please review them before use:

### When Fact-Checkers Haven't Covered the Topic
If searches return no relevant results:

1. Try broader search terms
2. Try related claims that fact-checkers may have covered
3. If still no results, check if the content is recent (3 days or less)
4. **For fresh content (≤3 days old)**:
   * Acknowledge: "This is very recent content. Professional fact-checkers typically need a few days to verify claims."
   * If scheduling tools are available: Schedule a follow-up fact-check for 3 days later
   * If scheduling is not available: Suggest the user returns in 3 days for updated verification
   * Offer to do preliminary general web research in the meantime
5. **For older content**: Acknowledge "Professional fact-checkers haven't specifically addressed this claim"
6. Offer to do general web research instead
7. Consider if the claim is too obscure or too local for major fact-checkers

### Contradicting Fact-Checkers
If fact-checkers disagree:

1. Present all perspectives fairly
2. Note the disagreement explicitly
3. Consider if they're addressing slightly different aspects
4. Look for consensus on specific sub-points
5. Don't force a conclusion if the evidence is genuinely mixed

### Outdated Information
If fact-checks are old but the claim is current:

1. Note the publication dates
2. Search for more recent fact-checks
3. Consider if circumstances have changed
4. Acknowledge if using older sources due to lack of recent coverage

### Language Barriers
If key fact-checkers are in languages you don't fully understand:

1. Use web_fetch to retrieve the content
2. Focus on verdicts, ratings, and conclusion sections which are often clear
3. Use any English summaries or abstracts
4. Acknowledge limitations if language creates uncertainty

### Bias Concerns
Users may question fact-checker reliability:

1. Stick to well-established, internationally recognized services
2. Present findings from multiple fact-checkers to show consensus
3. Note if you're using fact-checkers from multiple countries/perspectives
4. Acknowledge that no source is perfect, but these are professional verification services

## 功能详解与边界条件

### 核心功能详解

1. **内容验证**：
   - **输入参数**：`content`（待验证内容），`mode`（内容处理模式，如json、text、markdown），`max_retries`（单步最大重试次数），`skip_steps`（跳过的步骤编号）。
   - **处理逻辑**：技能会根据提供的输入内容，按照指定模式进行处理，自动执行一系列验证步骤，包括数据提取、事实核查、结果分析等。
   - **输出结果**：返回包含验证结果、执行日志、执行状态和错误信息的JSON格式数据。

2. **延迟跟进**：
   - **输入参数**：与内容验证相同。
   - **处理逻辑**：在无法立即完成验证时，自动创建延迟跟进任务，并在指定时间后再次尝试验证。
   - **输出结果**：返回包含延迟跟进任务信息和执行状态的JSON格式数据。

### 边界条件

1. **输入内容长度**：单个验证内容的长度限制为10000个字符。
2. **并发限制**：同一时间最多只能执行5个并发验证任务。
3. **字符编码**：输入内容必须使用UTF-8编码。
4. **延迟时间**：延迟跟进任务的最短延迟时间为1小时，最长延迟时间为7天。
5. **重试次数**：单步最大重试次数默认为2次。
6. **跳过步骤**：跳过的步骤编号必须为整数，且步骤编号范围应在1到总步骤数之间。
7. **输出格式**：返回的JSON格式数据必须符合预定义的格式规范。
8. **LLM API限制**：使用LLM API进行内容处理时，每秒最多调用次数限制为5次。

### 错误处理

1. **配置错误**：参数缺失或格式错误时，返回错误信息并提示用户检查配置。
2. **运行时错误**：运行环境不满足要求或权限不足时，返回错误信息并提示用户检查环境配置。
3. **网络错误**：连接超时或不可达时，返回错误信息并提示用户检查网络连接。
4. **输入内容格式不正确**：输入内容格式不符合要求时，返回错误信息并提示用户检查输入格式。
5. **执行结果与预期不符**：执行结果与预期不符时，返回错误信息并提示用户检查输入内容或处理逻辑。
6. **LLM响应超时或无响应**：LLM响应超时或无响应时，返回错误信息并提示用户检查LLM服务状态。
7. **API Key配置错误**：API Key配置错误或未配置时，返回错误信息并提示用户检查API Key配置。
8. **权限不足**：执行操作时权限不足，返回错误信息并提示用户检查权限设置。

### 性能指标

1. **响应时间**：验证任务的平均响应时间约为3秒。
2. **处理速度**：每秒可处理约2个验证任务。
3. **延迟时间**：延迟跟进任务的平均延迟时间为2小时。
4. **并发处理能力**：最多可同时处理5个并发验证任务。
5. **API调用限制**：每秒最多调用LLM API 5次。


## 差异化优势

### 与同类方案对比

1. **手动操作**：与手动操作相比，"verify-claims 4"技能极大地简化了事实核查的过程。手动操作通常需要花费大量时间在搜索、阅读和验证信息上，而"verify-claims 4"技能可以自动完成这些步骤，将核查时间从小时缩短到分钟。
   
2. **通用事实核查工具**：虽然一些通用的事实核查工具也能提供自动化的核查功能，但它们通常缺乏"verify-claims 4"技能的高级特性，如复杂的流程编排、条件分支处理和延迟跟进功能。"verify-claims 4"技能通过这些特性，为用户提供了更灵活和高效的核查体验。

3. **其他自动化工具**：与其他自动化工具相比，"verify-claims 4"技能专注于事实核查这一特定领域，因此在核查准确性和效率上具有专长。而一些通用自动化工具可能需要用户进行额外的配置和定制，才能适应特定的核查需求。

### 独特功能

1. **复杂的流程编排**："verify-claims 4"技能支持用户通过可视化界面进行复杂的流程编排，这使得用户能够轻松设计出满足特定核查需求的自动化流程。

2. **条件分支与异常重试**：技能能够根据预设条件进行分支处理，并在遇到异常时自动重试，这提高了核查的鲁棒性和准确性。

3. **定时触发与事件驱动**：用户可以设置技能在特定时间自动触发，或者根据事件触发核查，这使得技能能够适应不同的业务场景。

4. **分布式任务调度与负载均衡**：技能支持分布式任务调度和负载均衡，这提高了处理大规模核查任务的能力。

5. **执行日志与审计追踪**：技能提供详细的执行日志和审计追踪功能，帮助用户了解核查过程，并在出现问题时进行调试。

### 效率提升

使用"verify-claims 4"技能，平均可以将事实核查的效率提升5-10倍。例如，原本需要30分钟完成的核查任务，现在只需3-6分钟。

### 应用场景创新

1. **新闻编辑室**：新闻编辑室可以使用"verify-claims 4"技能对新闻稿中的事实进行快速核查，确保新闻的准确性和可靠性。

2. **市场研究**：市场研究人员可以使用技能核查市场报告中的数据，确保报告的准确性。

3. **法律咨询**：律师可以使用技能核查案件中的事实，提高案件分析的准确性和效率。


## 技术细节与实现说明

### 技术架构

"verify-claims 4"技能采用模块化设计，其技术架构主要包括以下几个核心模块：

1. **输入解析模块**：负责解析输入参数，包括内容、模式、最大重试次数和跳过步骤等。
2. **事实核查模块**：根据输入内容，调用LLM API进行事实核查，包括数据提取、事实核查、结果分析等。
3. **流程控制模块**：根据预设流程，控制各个模块的执行顺序，实现复杂的流程编排。
4. **延迟跟进模块**：在无法立即完成验证时，自动创建延迟跟进任务，并在指定时间后再次尝试验证。
5. **日志记录模块**：记录技能的执行过程，包括执行步骤、状态、耗时等信息，方便用户进行调试和审计。

核心算法主要包括：

- **自然语言处理（NLP）**：用于提取输入内容中的关键信息，如实体、关系等。
- **事实核查算法**：根据提取的信息，调用LLM API进行事实核查，并返回核查结果。
- **流程控制算法**：根据预设流程，控制各个模块的执行顺序，实现复杂的流程编排。

### 参数说明

| 参数名 | 类型 | 取值范围 | 默认值 | 说明 |
|---:|---:|---:|---:|---:|
| content | string | - | - | 待验证内容 |
| mode | string | json/text/markdown | text | 内容处理模式，可选json、text、markdown |
| max_retries | integer | 1-10 | 2 | 单步最大重试次数 |
| skip_steps | array | - | [] | 跳过的步骤编号（用于断点续传） |

### 返回值

返回值的数据结构如下：

```json
{
  "success": boolean,
  "data": {
    "final_result": {
      "claims_result": string,
      "claims_metadata": string,
      "claims_status": string
    },
    "execution_log": array,
    "total_duration_ms": integer,
    "gates_passed": integer,
    "gates_total": integer
  },
  "error": string
}
```

字段含义：

- `success`：表示操作是否成功，true表示成功，false表示失败。
- `data`：包含最终结果、执行日志、执行状态和错误信息。
- `final_result`：包含核查结果、元数据和状态。
- `execution_log`：包含执行步骤、名称、状态、耗时和输出摘要。
- `total_duration_ms`：表示执行总耗时（毫秒）。
- `gates_passed`：表示通过的门控数量。
- `gates_total`：表示总门控数量。
- `error`：表示错误信息，如果操作成功，则为null。

### 代码示例

**示例1：基础用法**

```python
from verify_claims import verify_claims

# 输入参数
input_data = {
    "content": "示例数据",
    "mode": "text",
    "max_retries": 2,
    "skip_steps": []
}

# 调用verify_claims技能
result = verify_claims(input_data)

# 打印结果
print(result)
```

**示例2：进阶用法**

```python
from verify_claims import verify_claims

# 输入参数
input_data = {
    "content": "示例数据",
    "mode": "json",
    "max_retries": 3,
    "skip_steps": [1, 3]
}

# 调用verify_claims技能
result = verify_claims(input_data)

# 打印结果
print(result)
```


## 安全注意事项

### API密钥与认证

**密钥管理**：为确保API密钥的安全性，请将其存储在安全的配置文件中，并确保配置文件不会被泄露到版本控制系统。同时，建议定期更换API密钥，以降低密钥泄露的风险。

**认证方式**：所有API调用都应使用HTTPS协议进行加密传输，确保数据传输过程中的安全性。对于认证，请使用OAuth 2.0或其他安全的认证机制，确保只有授权用户才能访问API。

**权限要求**：API密钥应设置合理的权限限制，仅授予必要的操作权限，避免未授权访问和滥用。

### 数据安全

**数据传输**：所有数据传输都应使用HTTPS协议进行加密，确保数据在传输过程中的安全性。

**数据存储**：敏感数据（如API密钥、用户数据等）应存储在加密的数据库中，并定期备份数据库，以防数据丢失。

**数据处理**：在处理数据时，请遵循最小权限原则，确保只有处理数据所必需的权限。同时，对敏感数据进行脱敏处理，防止信息泄露。

### 风险评估

**潜在安全风险**：
- API密钥泄露：可能导致未授权访问和滥用。
- 数据泄露：可能导致敏感信息泄露。
- 恶意攻击：可能导致系统崩溃或数据损坏。

**缓解措施**：
- 定期更新系统，修复安全漏洞。
- 实施访问控制策略，限制用户权限。
- 定期进行安全审计，及时发现和修复安全问题。

### 安全最佳实践

1. 使用HTTPS协议进行数据传输，确保数据传输过程中的安全性。
2. 定期更换API密钥，并确保配置文件不被泄露。
3. 对敏感数据进行脱敏处理，防止信息泄露。
4. 实施访问控制策略，限制用户权限。
5. 定期进行安全审计，及时发现和修复安全问题。

