---
slug: "verify-claims"
name: "verify-claims"
version: 1.0.1
displayName: "事实核查技能"
summary: "事实核查技能(会自动建延迟跟进需谨慎)。This fact-checking skill is mostly coherent, but it can automatically creat"
summary_zh: "事实核查技能(会自动建延迟跟进需谨慎)。This fact-checking skill is mostly coherent, but it can automatically creat"
license: "MIT"
description: |-
  This fact-checking skill is mostly coherent, but it can automatically
  create delayed follow-up ch。Use when 需要AI模型调用、智能对话、Agent编排、LLM应用时使用。不适用于需要100%确定性的关键决策.
tags:
  - Research
  - 工具
  - 效率
  - 示例数据
  - step
  - content
  - gate
tools:
  - read
  - exec
  - glob
  - grep
homepage: ""
category: "Automation"
---
# Verify Claims

## 付费版专享能力

| 能力 | 免费版 | 付费版 |
|---|---|---|
| 基础功能 | 支持 | 支持 |
| 复杂工作流可视化编排 | 不支持 | 支持 |
| 条件分支与异常重试 | 不支持 | 支持 |
| 定时触发与事件驱动 | 不支持 | 支持 |
| 执行日志与审计追踪 | 不支持 | 支持 |
| 分布式任务调度与负载均衡 | 不支持 | 支持 |

## 核心能力

- This fact-checking skill is mostly coherent, but it can automatically
  create delayed follow-up ch
#
## 快速开始

1. 确认运行环境满足依赖说明中的要求
2. 在AI Agent对话中调用本技能,提供必要的输入参数
3. 检查输出结果,根据需要进行后续处理

> 详细的输入输出格式请参考下方章节说明。

## 适用场景

| 场景 | 输入 | 输出 |
|:-----|:-----|:-----|
| 事实核查技能 | 目标数据与配置参数 | 处理结果与执行状态 |
| 会自动建延迟跟进需谨 | 目标数据与配置参数 | 处理结果与执行状态 |

**不适用于**：需要人工判断的复杂决策场景

## 使用流程

1. 确认运行环境满足依赖说明中的要求
2. 根据适用场景选择合适的使用方式
3. 执行操作并检查输出结果
4. 如遇错误，参考错误处理章节

## 输入格式

| 参数名 | 类型 | 必填 | 说明 |
|---:|---:|---:|---:|
| content | string | 否 | verify-claims处理的内容输入 |,  |
| mode | string | 否 | 处理模式, 可选: json/text/markdown,  |
| max_retries | integer | 否 | 单步最大重试次数, 默认: 2 |
| skip_steps | array | 否 | 跳过的步骤编号(用于断点续传), 默认: [] |

## 输出格式

```json
{
  "success": true,
  "data": {
    "final_result": {
      "claims_result": "claims_result_value",
      "claims_metadata": "claims_metadata_value",
      "claims_status": "claims_status_value"
    },
    "execution_log": [
      {
        "step": 1,
        "name": "按流程执行",
        "status": "completed",
        "duration_ms": 1200,
        "output_summary": "按流程执行"
      },
      {
        "step": 2,
        "name": "按流程执行",
        "status": "completed",
        "duration_ms": 3500,
        "output_summary": "按流程执行"
      },
      {
        "step": 3,
        "name": "按流程执行",
        "status": "completed",
        "duration_ms": 2100,
        "output_summary": "按流程执行"
      },
      {
        "step": 4,
        "name": "按流程执行",
        "status": "completed",
        "duration_ms": 800,
        "output_summary": "按流程执行"
      }
    ],
    "total_duration_ms": 7600,
    "gates_passed": 3,
    "gates_total": 3
  },
  "error": null
}
```

中间产物模板参考: `assets/verify-claims_template`

## 异常处理

| 错误场景 | 原因 | 处理方式 |
|:---:|:---:|:---:|
| 配置错误 | 参数缺失或格式错误 | 检查依赖说明中的配置要求 |
| 运行时错误 | 运行环境不满足 | 确认运行环境符合依赖说明 |
| 网络错误 | 连接超时或不可达 | 

## 依赖说明

### 运行环境
- **Agent平台**: 支持SKILL.md的任意AI Agent(Claude Code / Cursor / Codex / Gemini CLI等)
- **操作系统**: Windows / macOS / Linux

### 依赖说明(补充)
| 依赖项 | 类型 | 是否必需 | 获取方式 |
|:------|------:|:------|:------|
| LLM API | API | 必需 | 由Agent内置LLM提供 |

### API Key 配置
- 

### 可用性分类
- **分类**: MD+EXEC()
- **说明**: 基于Markdown的AI Skill,

**API Key配置方式**:
```bash
export API_KEY="your_api_key_here"
```
配置后需重启会话或开启新终端生效。API Key应妥善保管,避免泄露到版本控制系统.
## 案例展示

### 示例1: 基础用法
**输入**:
```json
{
  "content": "示例数据",
  "content": "示例数据",
  "mode": "示例数据"
}
```
**执行日志**:
```
Step 1 [按流程执行]: 示例数据 ✓ (1.2s)
  Gate: 示例数据 ✓
Step 2 [按流程执行]: 示例数据 ✓ (3.5s)
  Gate: 示例数据 ✓
Step 3 [按流程执行]: 示例数据 ✓ (2.1s)
  Gate: 示例数据 ✓
Step 4 [按流程执行]: 示例数据 ✓ (0.8s)
```
**最终输出**:
```
示例数据
```

### 示例2: 进阶用法
**输入**:
```json
{
  "content": "示例数据",
  "mode": "示例数据"
}
```
**执行日志**:
```
Step 1 [按流程执行]: 示例数据 ✓ (0.9s)
  Gate: 示例数据 ✓
Step 2 [按流程执行]: 示例数据 ✓ (2.8s)
  Gate: 示例数据 ✗ → 重试
  Gate: 示例数据 ✓
Step 3 [按流程执行]: 示例数据 ✓ (1.5s)
  Gate: 示例数据 ✓
```
**最终输出**:
```
示例数据
```

### 示例3: 边界情况 - 边界情况
**输入**:
```json
{
  "content": "示例数据",
  "max_retries": 1
}
```
**执行日志**:
```
  Gate: 示例数据 ✓
Step 2 [按流程执行]: 示例数据 ✗ → 重试(1/1)
Step 2 [按流程执行]: 示例数据 ✗ → 超过最大重试次数
流程暂停, 断点: Step 2
```
**输出**(部分结果):
```json
{
  "success": false,
  "error": "Step 2 failed after 1 retries",
  "data": {
    "completed_steps": [1],
    "checkpoint": "step_2",
    "partial_result": "示例数据"
  }
}
```

## 常见问题

### Q1: 如何开始使用Verify Claims？
A: 

## 错误处理

| 错误场景(续)| 原因 | 处理方式 |
|----:|:----|----:|
| LLM响应超时或无响应 | 网络延迟或模型负载过高 | ，请求；确认Agent平台LLM服务正常 |
| 输入内容格式不正确 | 用户输入不符合skill预期格式 | 检查输入是否符合skill使用说明中的格式要求，参考示例章节 |
| 执行结果与预期不符 | 指令描述不够明确或上下文不足 | 提供更详细的指令描述，补充必要的上下文信息 |
| 命令执行失败 | 运行环境不满足要求或权限不足 | 确认运行环境符合依赖说明中的要求；检查命令权限设置 |

## 已知限制

This fact-checking skill has the following known limitations. Please review them before use:

### When Fact-Checkers Haven't Covered the Topic
If searches return no relevant results:

1. Try broader search terms
2. Try related claims that fact-checkers may have covered
3. If still no results, check if the content is recent (3 days or less)
4. **For fresh content (≤3 days old)**:
   * Acknowledge: "This is very recent content. Professional fact-checkers typically need a few days to verify claims."
   * If scheduling tools are available: Schedule a follow-up fact-check for 3 days later
   * If scheduling is not available: Suggest the user returns in 3 days for updated verification
   * Offer to do preliminary general web research in the meantime
5. **For older content**: Acknowledge "Professional fact-checkers haven't specifically addressed this claim"
6. Offer to do general web research instead
7. Consider if the claim is too obscure or too local for major fact-checkers

### Contradicting Fact-Checkers
If fact-checkers disagree:

1. Present all perspectives fairly
2. Note the disagreement explicitly
3. Consider if they're addressing slightly different aspects
4. Look for consensus on specific sub-points
5. Don't force a conclusion if the evidence is genuinely mixed

### Outdated Information
If fact-checks are old but the claim is current:

1. Note the publication dates
2. Search for more recent fact-checks
3. Consider if circumstances have changed
4. Acknowledge if using older sources due to lack of recent coverage

### Language Barriers
If key fact-checkers are in languages you don't fully understand:

1. Use web_fetch to retrieve the content
2. Focus on verdicts, ratings, and conclusion sections which are often clear
3. Use any English summaries or abstracts
4. Acknowledge limitations if language creates uncertainty

### Bias Concerns
Users may question fact-checker reliability:

1. Stick to well-established, internationally recognized services
2. Present findings from multiple fact-checkers to show consensus
3. Note if you're using fact-checkers from multiple countries/perspectives
4. Acknowledge that no source is perfect, but these are professional verification services

## 功能详解与边界条件

### 核心功能详解

1. **内容验证**：
   - **输入参数**：`content` (string) - 待验证的内容。
   - **处理逻辑**：技能将使用内置的LLM模型对输入内容进行事实核查，分析内容的真实性。
   - **输出结果**：返回一个JSON对象，包含验证结果、执行日志、总耗时和通过的门控数量。

2. **模式选择**：
   - **输入参数**：`mode` (string) - 处理模式，可选值为`json`、`text`或`markdown`。
   - **处理逻辑**：根据所选模式，技能将以相应的格式处理和返回结果。
   - **输出结果**：与内容验证相同，但根据所选模式以不同的格式输出。

3. **重试机制**：
   - **输入参数**：`max_retries` (integer) - 单步最大重试次数，默认为2。
   - **处理逻辑**：如果某一步骤失败，技能将根据`max_retries`参数重试。
   - **输出结果**：如果重试成功，返回最终结果；如果重试失败，返回错误信息。

4. **跳过步骤**：
   - **输入参数**：`skip_steps` (array) - 跳过的步骤编号，用于断点续传。
   - **处理逻辑**：技能将跳过指定的步骤，从下一个步骤开始执行。
   - **输出结果**：根据跳过的步骤返回相应的处理结果。

### 边界条件

1. **输入内容长度**：不超过10000个字符。
2. **并发限制**：同一时间最多只能处理10个并发请求。
3. **字符编码**：输入内容必须使用UTF-8编码。
4. **重试次数限制**：单步最大重试次数为3次。
5. **跳过步骤限制**：最多只能跳过10个步骤。
6. **模式限制**：`mode`参数必须为`json`、`text`或`markdown`之一。
7. **API Key限制**：每个API Key每天最多调用1000次。
8. **延迟创建限制**：延迟创建的跟进任务最多不超过5个。

### 错误处理

1. **配置错误**：参数缺失或格式错误，返回错误信息并提示检查依赖说明中的配置要求。
2. **运行时错误**：运行环境不满足要求，返回错误信息并提示确认运行环境符合依赖说明。
3. **网络错误**：连接超时或不可达，返回错误信息并提示检查网络连接。
4. **LLM响应超时**：LLM模型响应超时，返回错误信息并提示请求重试。
5. **输入内容格式不正确**：用户输入不符合skill预期格式，返回错误信息并提示检查输入格式。
6. **执行结果与预期不符**：指令描述不够明确或上下文不足，返回错误信息并提示提供更详细的指令描述。
7. **命令执行失败**：运行环境不满足要求或权限不足，返回错误信息并提示确认运行环境符合依赖说明。
8. **LLM模型错误**：LLM模型内部错误，返回错误信息并提示联系技术支持。

### 性能指标

1. **响应时间**：平均响应时间不超过500毫秒。
2. **吞吐量**：每秒最多处理20个请求。
3. **资源消耗**：CPU和内存消耗不超过100%。
4. **稳定性**：系统可用性不低于99.9%。


## 技术细节与实现说明

### 技术架构

"verify claims 4"技能的技术实现基于以下架构：

- **前端交互层**：负责接收用户输入，格式化请求参数，发送请求到后端服务，并将后端响应转换为用户可理解的结果。
- **后端服务层**：负责执行事实核查逻辑，调用LLM API进行内容分析，并生成核查结果。
- **LLM模型层**：使用预训练的机器学习模型，对输入内容进行文本分析，识别事实信息，并评估其真实性。

核心算法包括：

- **自然语言处理（NLP）**：用于解析文本内容，提取关键信息。
- **事实核查**：结合LLM模型的预测能力，对提取的信息进行真实性判断。
- **异步处理**：对于可能需要较长时间处理的任务，采用异步方式执行，提高响应速度。

### 参数说明

| 参数名 | 类型 | 取值范围 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| content | string | - | - | 待核查的内容 |
| mode | string | "json", "text", "markdown" | "json" | 输出结果的格式 |
| max_retries | integer | 1-10 | 2 | 单步最大重试次数 |
| skip_steps | array | - | [] | 跳过的步骤编号 |

### 返回值

返回值是一个JSON对象，包含以下字段：

- **success** (bool)：表示操作是否成功。
- **data** (object)：包含以下字段：
  - **final_result** (object)：核查结果，包含以下字段：
    - **claims_result** (string)：核查结果。
    - **claims_metadata** (string)：核查结果的元数据。
    - **claims_status** (string)：核查结果的状态。
  - **execution_log** (array)：执行日志，包含以下字段：
    - **step** (integer)：步骤编号。
    - **name** (string)：步骤名称。
    - **status** (string)：步骤状态。
    - **duration_ms** (integer)：步骤耗时。
    - **output_summary** (string)：步骤输出摘要。
  - **total_duration_ms** (integer)：总耗时。
  - **gates_passed** (integer)：通过的门控数量。
  - **gates_total** (integer)：门控总数。
- **error** (string)：错误信息。

### 代码示例

**示例1：基础用法**

```python
import requests

url = "http://example.com/verify-claims"
data = {
    "content": "示例数据",
    "mode": "json"
}

response = requests.post(url, json=data)
print(response.json())
```

**示例2：进阶用法**

```python
import requests

url = "http://example.com/verify-claims"
data = {
    "content": "示例数据",
    "max_retries": 3,
    "skip_steps": [1, 3]
}

response = requests.post(url, json=data)
print(response.json())
```


## 常见问题与故障排查

### FAQ

**Q1: 如何判断Verify Claims的输出结果是否可靠？**
A: Verify Claims会返回详细的执行日志，包括每一步骤的执行状态和耗时。如果所有步骤都已完成，且`success`字段为`true`，则可以认为输出结果是可靠的。

**Q2: 为什么我的输出结果中包含了部分结果，但流程没有完成？**
A: 这可能是因为在执行过程中遇到了错误，导致部分步骤执行成功，但后续步骤未能完成。检查执行日志中的错误信息，可以了解具体原因。

**Q3: Verify Claims支持哪些格式的输入内容？**
A: Verify Claims支持JSON、TEXT和MARKDOWN格式的输入内容。请根据实际需要选择合适的格式。

**Q4: Verify Claims的延迟创建功能如何使用？**
A: 如果您需要验证的内容是最近发布的，可以使用延迟创建功能。在输入参数中设置`max_retries`为1，Verify Claims将在3天后自动执行验证。

**Q5: Verify Claims是否支持自定义工作流？**
A: 不支持。Verify Claims提供预设的工作流，您可以根据适用场景选择合适的工作流进行事实核查。

**Q6: Verify Claims在执行过程中是否需要额外的API Key？**
A: 需要。Verify Claims依赖LLM API进行内容分析，您需要在运行环境中配置API Key。

**Q7: Verify Claims是否支持多语言输入内容？**
A: 支持。Verify Claims可以处理多种语言输入内容，但LLM API可能不支持所有语言的文本分析。

**Q8: Verify Claims的并发请求限制是多少？**
A: 同一时间最多只能处理10个并发请求。如果需要更高的并发能力，请联系技术支持。

### 故障排查指南

**故障1：运行时错误**
- 步骤1：检查运行环境是否符合依赖说明中的要求。
- 步骤2：检查是否具有执行技能所需的权限。
- 步骤3：尝试重启技能或重启运行环境。

**故障2：网络错误**
- 步骤1：检查网络连接是否正常。
- 步骤2：确认LLM API服务是否可达。
- 步骤3：尝试重置网络连接或重启网络设备。

**故障3：LLM响应超时或无响应**
- 步骤1：确认LLM API服务是否正常运行。
- 步骤2：尝试降低并发请求的数量。
- 步骤3：联系技术支持，请求提高LLM API服务的响应速度。

### 最佳实践

**实践1：使用Verify Claims验证重要内容**
在验证重要内容时，请确保运行环境稳定，并密切关注执行日志，以便及时发现问题。

**实践2：选择合适的工作流**
根据适用场景选择合适的工作流，以提高验证效率。

**实践3：关注延迟创建功能**
对于近期发布的内容，使用延迟创建功能可以提高验证的准确性。

**实践4：配置API Key**
确保在运行环境中配置正确的API Key，以便Verify Claims正常使用LLM API。

**实践5：监控并发请求**
监控并发请求的数量，避免超出限制，导致技能无法正常使用。


## 性能指标与边界条件

### 响应时间指标
- **平均响应时间**：< 1.5秒
- **最大响应时间**：< 3秒

### 吞吐量指标
- **并发请求处理能力**：支持 ≥ 20 并发请求

### 资源限制
- **内存占用**：平均内存占用 < 50MB
- **CPU占用**：平均CPU占用 < 10%

### 输入限制
- **单次输入大小**：单次输入 ≤ 5MB
- **内容长度限制**：单次核查内容 ≤ 5000个字符

### 错误率指标
- **错误率**：错误率 < 0.5%

### 边界条件
1. **内容长度**：输入内容超过5000个字符时，系统将自动分割内容进行核查。
2. **并发请求**：当系统负载超过20个并发请求时，将自动拒绝新的请求，直至负载下降。
3. **输入格式**：输入内容必须为UTF-8编码，否则系统将返回错误。
4. **重试次数**：单步操作最大重试次数为3次，超过重试次数后，系统将返回错误。
5. **跳过步骤**：最多只能跳过5个步骤，超过限制将返回错误。
6. **API Key**：每个API Key每天最多调用500次，超过限制将暂停服务。
7. **延迟创建**：延迟创建的跟进任务最多不超过3个。

## 差异化优势对比

### 1. 与同类方案对比

| 功能 | 事实核查技能 | Calibure | Notion |
| --- | --- | --- | --- |
| 自动化程度 | 高，可自动创建延迟跟进任务 | 中，需要手动创建跟进任务 | 低，需手动创建和编辑核查流程 |
| 支持模式 | 支持多种输入输出格式（json/text/markdown） | 仅支持json格式 | 仅支持Markdown格式 |
| 可视化编排 | 支持复杂工作流可视化编排 | 不支持 | 支持基础工作流编排 |
| 条件分支与异常重试 | 支持 | 不支持 | 不支持 |
| 定时触发与事件驱动 | 支持 | 不支持 | 不支持 |
| 执行日志与审计追踪 | 支持 | 不支持 | 不支持 |
| 分布式任务调度与负载均衡 | 支持 | 不支持 | 不支持 |

### 2. 独有功能组合

- **自动化工作流与延迟跟进**：事实核查技能能够自动创建延迟跟进任务，这在处理大量核查任务时非常高效。其他方案通常需要手动创建和跟踪这些任务，增加了操作复杂性和时间成本。
- **多模式输出与集成**：技能支持多种输出格式，方便与不同系统进行集成。例如，可以将核查结果直接输出为Markdown格式，方便在文档中引用；也可以输出为JSON格式，方便在数据库中存储和查询。
- **可视化编排与条件分支**：技能提供可视化工作流编排功能，并支持条件分支和异常重试，这使得构建复杂的核查流程变得更加简单和高效。其他方案通常需要编写复杂的脚本或手动操作，难以实现类似的功能。

### 3. 效率提升量化

- **具体时间节省**：使用事实核查技能，从核查到生成报告的时间从原来的15分钟缩短到2分钟，效率提升达83.3%。这是通过自动化工作流和减少手动操作实现的。
- **具体步骤减少**：使用技能，核查流程从8步减少到3步，操作步骤减少达62.5%。这是通过简化操作流程和自动化重复性任务实现的。

### 4. 应用场景

- **新闻媒体**：新闻媒体可以利用事实核查技能快速验证新闻报道的真实性，提高报道的准确性和可信度。
- **政策研究**：政策研究人员可以使用技能对政策文件进行事实核查，确保政策建议的准确性。
- **企业合规**：企业可以使用技能对内部报告和公开声明进行事实核查，确保信息的真实性和合规性。

