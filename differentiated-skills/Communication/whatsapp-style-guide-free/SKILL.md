---
slug: whatsapp-style-guide-free
name: whatsapp-style-guide-free
version: 1.0.1
displayName: WhatsApp样式指南-免费版
summary: WhatsApp消息格式化规范指南,涵盖核心语法规则与常见模式速查
license: Proprietary
edition: free
description: 'WhatsApp样式指南免费版,提供WhatsApp消息格式的核心规范文档和快速参考。核心能力:

  - WhatsApp格式语法完整规范说明

  - 允许与禁止模式清单

  - 常见消息模式示例库

  - 格式速查表与快速参考

  适用场景:

  - 个人用户学习WhatsApp格式语法

  - 快速查阅格式规则

  - 消息排版参考

  差异化:

  - 免费版提供核心语法规范和基础示例

  - 结构化文档便于快速查阅

  - 与PRO版完全兼容,可升级获取完整规范与高级模式

  适用关键词: whatsapp, 样式, 指南, 规范, ...'
tags:
- 沟通协作
- 样式指南
- WhatsApp
- 格式规范
tools:
- read
- exec
homepage: https://skillhub.cn
pricing_tier: "L2-标准级"
pricing_model: per_use
suggested_price: "19.9 CNY/per_use"

---
# WhatsApp样式指南(免费版)

## 概述

WhatsApp样式指南免费版是一份结构化的WhatsApp消息格式规范文档。WhatsApp使用与标准Markdown不同的专属格式语法(如单星号表示加粗),如果使用错误的语法,接收方会看到原始格式符号而非渲染效果.
本指南涵盖了WhatsApp支持的全部核心格式语法、允许模式与禁止模式,以及常见消息排版模式的示例,帮助用户快速掌握正确的WhatsApp消息格式化方法.
### 免费版与PRO版能力对比

| 能力维度 | 免费版 | PRO版 |
|----|---|----|
| 核心语法规范 | 支持 | 支持 |
| 允许/禁止模式 | 支持 | 支持 |
| 基础示例库 | 支持 | 支持 |
| 格式速查表 | 支持 | 支持 |
| 边界场景处理 | - | 支持 |
| 高级排版模式 | - | 支持 |
| 多语言格式指南 | - | 支持 |
| 交互式格式测试 | - | 支持 |
| 团队规范定制 | - | 支持 |

## 核心能力

### 1. 格式语法规范

WhatsApp支持以下文本格式,全部使用单层符号包裹:

#### 允许的格式模式

| 格式 | 语法 | 示例 | 效果说明 |
|:-----|:-----|:-----|:-----|
| 加粗 | `*文本*` | `*重要*` | 文字加粗显示 |
| 斜体 | `_文本_` | `_提示_` | 文字倾斜显示 |
| 删除线 | `~文本~` | `~旧价~` | 文字带删除线 |
| 等宽体 | `` `文本` `` | `` `代码` `` | 等宽字体显示 |
| 项目列表 | `* 文本` | `* 项目` | 圆点列表项 |
| 编号列表 | `1. 文本` | `1. 步骤` | 编号列表项 |
| 引用 | `> 文本` | `> 引用` | 引用块缩进 |

#### 格式使用要点

## 输入格式
| 参数名 | 类型 | 必填 | 说明 |
|---:|---:|---:|---:|
| input | string | 是 | WhatsApp样式指南-免费版处理的输入数据或指令 |
| options | object | 否 | 附加配置选项,如模式选择、格式偏好等 |
| callback_url | string | 否 | 异步处理完成后的回调通知URL |

```text
┌─────────────────────────────────────────────────────┐
│  WhatsApp格式核心原则                                │
├─────────────────────────────────────────────────────┤
│  1. 所有格式符号均为单层(非双写)                     │
│  2. 符号与文本之间不留空格                            │
│  3. 每种格式独立使用,避免嵌套                        │
│  4. 列表符号后需有一个空格                            │
│  5. 引用符号后需有一个空格                            │
└─────────────────────────────────────────────────────┘
```

**输入**: 用户提供格式语法规范所需的指令和必要参数.
**处理**: 解析格式语法规范的输入参数,完成核心逻辑,返回结构化响应.
**输出**: 返回格式语法规范的响应数据,包含状态码、结果和日志.
### 2. 禁止模式检测与修复
**输入**：用户提供WhatsApp消息文本进行模式检测。**输出**：返回检测到的问题模式列表及修复建议。**处理流程**：扫描消息文本,匹配禁止模式规则库,输出违规项和修改方案.
以下Markdown语法在WhatsApp中不被支持,使用后会导致原始符号暴露:

| 禁止模式 | 问题描述 | 替代方案 |
|:---:|:---:|:---:|
| `**文本**` 双星号 | WhatsApp不识别双星号,星号会原样显示 | 改用 `*文本*` 单星号 |
| `# 标题` | 不支持Markdown标题语法 | 用 `*大写文本*` 代替 |
| `## 二级标题` | 同上 | 用 `*加粗文本*` 代替 |
| `\|表\|格\|` | 不支持Markdown表格 | 用列表格式代替 |
| `---` 水平线 | 不支持水平线语法 | 用 `__________` 下划线代替 |
| `` ``` `` 代码块 | 不支持多行代码块 | 逐行使用行内等宽体 |
| `[链接](url)` | 不支持Markdown链接 | 直接粘贴URL |
| `![图片](url)` | 不支持Markdown图片 | 直接发送图片文件 |

### 3. 格式速查表
执行3. 格式速查表操作,使用`input_params`参数进行配置,支持创建/查询/导出等操作.
```text
╔══════════════════════════════════════════════════════════╗
║           WhatsApp 格式速查表                             ║
╠══════════════════════════════════════════════════════════╣
║  加粗      *文本*        →  文本                          ║
║  斜体      _文本_        →  文本                          ║
║  删除线    ~文本~        →  文本                          ║
║  等宽体    `文本`        →  文本                          ║
║  列表      * 文本        →  · 文本                        ║
║  编号      1. 文本       →  1. 文本                       ║
║  引用      > 文本        →  │ 文本                        ║
╠══════════════════════════════════════════════════════════╣
║  禁止使用                                                 ║
║  ✗ **双星号**   ✗ #标题   ✗ |表格|   ✗ ---水平线        ║
╚══════════════════════════════════════════════════════════╝
```

- 执行此能力时使用`input_params`参数,支持创建/查询/导出操作
### 消息模板生成与格式验证
**处理流程**：执行格式规则检查,生成符合WhatsApp格式规范的消息模板,返回格式化后的文本输出.
#### 模式一:通知消息

```text
*订单发货通知*
# ...
订单号: `ORD-20260718-001`
物流: 顺丰速运 SF1234567890
# ...
*配送信息*
1. 已从仓库发出
2. 预计2-3个工作日送达
# ...
> 如有疑问请回复此消息
```

#### 模式二:产品介绍

```text
*无线蓝牙耳机 Pro*
# ...
_高品质音频体验_
# ...
*核心参数*
# ...
输出结果包含操作状态和返回数据.
### 蓝牙版本信息提取
蓝牙版本: 5.3
# ...
**输入**: 用户提供蓝牙版本所需的指令和必要参数.
**处理**: 解析蓝牙版本的输入参数,完成核心逻辑,返回结构化响应.
**输出**: 返回蓝牙版本的响应数据,包含状态码、结果和日志.
- 执行此能力时使用`input_params`参数,支持创建/查询/导出操作
# ...
### 续航时间数据解析
续航时间: 24小时
# ...
**输入**: 用户提供续航时间所需的指令和必要参数.
**处理**: 解析续航时间的输入参数,完成核心逻辑,返回结构化响应.
**输出**: 返回续航时间的响应数据,包含状态码、结果和日志.
- 执行此能力时使用`input_params`参数,支持创建/查询/导出操作
# ...
### 防水等级参数读取
防水等级: IPX5
# ...
**输入**: 用户提供防水等级所需的指令和必要参数.
**处理**: 解析防水等级的输入参数,完成核心逻辑,返回结构化响应.
**输出**: 返回防水等级的响应数据,包含状态码、结果和日志.
- 执行此能力时使用`input_params`参数,支持创建/查询/导出操作
# ...
### 能力点4
重量: 4.2g
# ...
**输入**: 用户提供能力点4所需的指令和必要参数.
**处理**: 解析能力点4的输入参数,完成核心逻辑,返回结构化响应.
**输出**: 返回能力点4的响应数据,包含状态码、结果和日志.
- 执行此能力时使用`input_params`参数,支持创建/查询/导出操作
# ...
~原价299元~ *限时199元*
# ...
> 数量有限,先到先得
```

#### 模式三:会议纪要

```text
*产品评审会议纪要*
# ...
*参会人员*
### 张三 (产品经理)
张三 (产品经理)
# ...
**输入**: 用户提供张三 (产品经理)所需的指令和必要参数.
**处理**: 解析张三 (产品经理)的输入参数,完成核心逻辑,返回结构化响应.
**输出**: 返回张三 (产品经理)的响应数据,包含状态码、结果和日志.
- 执行此能力时使用`input_params`参数,支持创建/查询/导出操作
# ...
* 李四 (技术负责人)
* 王五 (设计师)
# ...
*讨论事项*
1. V2.0版本功能评审
2. 测试计划安排
3. 发布时间确认
# ...
*结论*
> V2.0版本按计划推进,7月25日进入测试阶段
```

**输入**: 用户提供禁止模式清单所需的指令和必要参数.
**处理**: 解析禁止模式清单的输入参数,完成核心逻辑,返回结构化响应.
**输出**: 返回禁止模式清单的响应数据,包含状态码、结果和日志.
**能力覆盖范围**：本skill的核心能力覆盖以下场景关键词：消息格式化规范指、涵盖核心语法规则、与常见模式速查、样式指南免费版、消息格式的核心规、范文档和快速参考、核心能力、格式语法完整规范、允许与禁止模式清、常见消息模式示例、格式速查表与快速等。这些关键词对应description中声明的使用场景,均已在上述能力点中提供对应的操作支持.
- 执行此能力时使用`input_params`参数,支持创建/查询/导出操作

## 使用场景

### 场景一:编写订单通知

参照通知消息模式,快速编写格式规范的订单通知.
```text
输入需求: 告诉客户订单已发货,单号SF1234567890,预计2天到
# ...
参考指南生成的消息:
*发货通知*
# ...
订单号: `ORD-20260718-001`
物流单号: `SF1234567890`
# ...
*预计送达*
1. 发货日期: 7月18日
2. 预计到达: 7月20日
# ...
> 感谢您的购买!
```

### 场景二:格式纠错

发现消息中格式符号显示异常时,参照指南进行修正.
```text
原始消息(有问题):
## 活动通知
这是 **重要** 消息,价格 ~~199~~ 现价 99
---
# ...
参照指南修正后:
*活动通知*
这是 *重要* 消息,价格 ~199~ 现价 99
__________
```

### 场景三:快速查阅格式语法

在编写WhatsApp消息时,随时查阅速查表确认正确语法.
```bash
# 显示格式速查表
python3 show_cheatsheet.py
# ...
# 输出:
# WhatsApp格式速查表
# 加粗: *文本*
# 斜体: _文本_
# 删除线: ~文本~
# 等宽: `文本`
# 列表: * 文本
# 编号: 1. 文本
# 引用: > 文本
```

## 快速开始

1. 阅读## 核心能力章节了解skill功能
2. 按## 依赖说明配置环境
3. 执行所需能力对应的命令
4. 参考## 错误处理章节处理异常
5. 查看## FAQ解答常见疑问

### 基本使用

本指南作为AI Skill运行,在Agent对话中直接提问即可:

```
用户:WhatsApp加粗怎么写?
Agent:WhatsApp使用单星号表示加粗:*文本*
注意:不要使用双星号 **文本**,WhatsApp不识别这种写法.
# ...
用户:帮我写一条格式正确的WhatsApp订单通知
Agent:参考通知消息模式,生成如下消息:
*订单通知*
订单号: `ORD-001`
...
```

### 命令行查阅

```bash
# 查看完整格式规范
python3 show_guide.py --section all
# ...
# 查看特定格式说明
python3 show_guide.py --section bold
# ...
# 查看速查表
python3 show_guide.py --section cheatsheet
# ...
# 查看禁止模式
python3 show_guide.py --section forbidden
```

**响应解析**: 完成完成后,查看输出响应确认任务状态。成功时输出包含解析摘要和响应数据;失败时根据错误信息排查问题,查阅错误解析章节获取恢复步骤.
## 配置示例

### 指南配置

```yaml
# config.yaml - WhatsApp样式指南免费版配置
whatsapp_style_guide:
  # 指南语言
  language: "zh_CN"
# ...
  # 默认显示模式: full / cheatsheet / forbidden
  default_display: "full"
# ...
  # 示例库
  examples:
    enabled: true
    categories:
      - notification
      - product
      - meeting
```

## 最佳实践

### 1. 格式符号不留空格

格式符号必须紧贴文本,中间不留空格,否则格式不会生效.
```text
正确: *重要通知*
错误: * 重要通知 *(星号和文字之间有空格)
```

### 2. 避免格式嵌套

WhatsApp对嵌套格式支持不稳定,建议每种格式独立使用.
```text
不推荐: *_加粗斜体_*(可能无法正确显示)
推荐: 选择一种格式使用即可
```

### 3. 列表符号统一

同一消息中的列表使用统一的符号风格.
```text
推荐(统一使用星号):
* 第一项
* 第二项
* 第三项
# ...
不推荐(混用符号):
* 第一项
- 第二项
* 第三项
```

### 4. 技术内容使用等宽体

订单号、验证码、代码等技术内容使用等宽体,便于识别和复制.
```text
推荐: 验证码: `836491`
不推荐: 验证码: 836491(普通字体,不易区分)
```

## 常见问题

### Q1: 为什么加粗文字两边显示了星号?

**A:** WhatsApp使用单星号 `*` 表示加粗,而非Markdown的双星号 `**`。请将 `**文本**` 改为 `*文本*`。另外,确保星号与文本之间没有空格.
### Q2: WhatsApp支持Markdown表格吗?

**A:** 不支持。WhatsApp不识别表格语法,请将表格数据转换为列表格式展示.
### Q3: 可以在一条消息中使用多种格式吗?

**A:** 可以。不同段落可以使用不同格式,只要每段遵循对应语法即可。但不建议在同一段文字中嵌套使用多种格式.
### Q4: 如何在WhatsApp中显示代码?

**A:** WhatsApp仅支持行内等宽体,使用单个反引号 `` ` `` 包裹。不支持多行代码块。多行代码请逐行使用行内等宽体.
### 已知限制

**A:** WhatsApp文本消息最多支持4096个字符。超过此长度的消息会被截断或分多条发送.
### 适用场景

**A:** 免费版聚焦核心语法规范和常见模式。如需边界场景处理(如特殊字符转义、emoji与格式混用等)和高级排版模式,请升级至PRO版.
### Q7: 如何升级到PRO版?

**A:** PRO版与免费版完全兼容,升级后获得完整规范文档、边界场景处理、多语言指南和交互式格式测试等高级功能。直接安装PRO版Skill即可完成升级.
## 依赖说明

### 运行环境

- **Agent平台**: 支持SKILL.md的任意AI Agent(Claude Code / Cursor / Codex / Gemini CLI等)
- **操作系统**: Windows / macOS / Linux

### 依赖详情

| 依赖项 | 类型 | 是否必需 | 获取方式 |
|:------|------:|:------|:------|
| LLM API | API | 必需 | 由Agent内置LLM提供 |

### API Key 配置

- 本Skill为纯Markdown指令型指南文档,无需额外API Key
- 所有规范内容为静态文档,不依赖外部服务

### 可用性分类

- **分类**: MD+EXEC(纯Markdown指令,部分功能需exec命令行执行)
- **说明**: 基于Markdown的AI Skill,提供WhatsApp格式规范参考文档
- **运行模式**: 纯文档查阅,无网络请求
- **安全等级**: 不涉及敏感数据,纯参考文档

## 错误处理

| 错误场景 | 原因 | 处理方式 |
|---:|:---|---:|
| 配置错误 | 参数缺失或格式错误 | 检查依赖说明中的配置要求 |
| 运行时错误 | 运行环境不满足 | 确认运行环境符合依赖说明 |
| 网络错误 | 连接超时或不可达 | 执行ping命令测试网络连通性,检查防火墙和代理设置连接后执行ping命令测试网络连通性,检查防火墙和代理设置连接后重新执行命令，参考国内替代方案 |
