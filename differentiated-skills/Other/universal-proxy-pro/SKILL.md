---
slug: universal-proxy-pro
name: universal-proxy-pro
version: 1.1.0
displayName: 网络配置管理工具(专业版)
summary: "网络配置管理工具，支持DNS配置、路由表管理、网卡配置、网络服务质量控制，适合运维工程师管理网络环境"
license: MIT
edition: pro
description: '网络配置管理工具专业版，面向运维工程师的网络环境管理工具。核心能力: DNS服务器配置与管理、路由表查看与修改、网卡配置管理、网络服务质量(QoS)控制、网络接口状态监控。适用场景: 多网卡环境配置管理、DNS服务器切换与优化、路由策略调整、网络带宽分配与限速、网络故障快速恢复。差异化: 专业版提供配置版本管理、批量部署、配置回滚、网络拓扑可视化，适合中大型运维团队管理复杂网络环境'
tags:
  - 网络配置
  - DNS管理
  - 路由管理
  - 网卡配置
  - 专业版
  - 工具
  - 运维
  - 效率
  - 自动化
  - bash
tools:
  - read
  - exec
  - write
homepage: ""
category: "Automation"
---
# 网络配置管理（专业版）

## 概述

网络配置管理工具专业版帮助运维工程师管理复杂的网络环境，提供DNS配置、路由管理、网卡配置、QoS控制、接口监控五大核心能力。
## 核心能力

| 能力 | 说明 |
|---|---|
| 连通性检测 | 使用ping/traceroute检测网络连通性 |
| 端口扫描 | 检测目标端口开放状态 |
| DNS诊断 | 检查DNS解析是否正常 |
| 路由追踪 | 分析网络路由路径与延迟 |

**技术实现要点**：核心能力基于`input_params`参数与`output_format`配置实现,支持创建/查询/修改/删除等操作模式,通过`config_options`进行运行时配置.

### 核心功能执行
用`input_params`参数进行配置.

**处理**: 解析核心功能执行的输入参数,完成核心逻辑,返回结构化响应.
**输出**: 返回核心功能执行的响应数据,包含状态码、结果和日志.
- 执行此能力时使用`input_params`参数,支持创建/查询/导出操作

### 参数配置与调用
用`config_options`参数进行配置.

**处理**: 解析参数配置与调用的输入参数,完成核心逻辑,返回结构化响应.
**输出**: 返回参数配置与调用的响应数据,包含状态码、结果和日志.
- 执行此能力时使用`config_options`参数,支持修改/重置/导入操作

### 结果处理与输出
用`output_format`参数进行配置.

**处理**: 解析结果处理与输出的输入参数,完成核心逻辑,返回结构化响应.
**输出**: 返回结果处理与输出的响应数据,包含状态码、结果和日志.
- 执行此能力时使用`output_format`参数,支持导出/保存/转换操作

## 使用场景

### 场景一：排查API连接超时

开发者调用外部API时连接超时，需诊断网络连通性.
```bash
# 检测目标服务器连通性
ping -c 4 api.example.com
# 追踪路由路径
traceroute -m 15 api.example.com
# 检查端口是否开放
nc -zv api.example.com 443
# 输出示例
# ✅ ping: 4个包全部到达,平均延迟 45ms
# ✅ 端口 443: 开放
# 📊 路由: 8跳,无丢包
```

### 场景二：DNS解析异常排查

域名无法解析或解析到错误IP时，诊断DNS配置.
```bash
# 检查DNS解析
nslookup example.com
# 使用指定DNS服务器解析
dig @8.8.8.8 example.com
# 检查当前DNS配置
cat /etc/resolv.conf
# 输出示例
# ✅ DNS解析正常
# 📊 解析服务器: 8.8.8.8
# 📊 解析结果: 93.184.216.34
```

### 场景三：端口连通性检测

检查目标服务的端口是否可达.
```bash
# 批量检测端口
for port in 80 443 8080 8443; do
  nc -zv -w3 target.host $port 2>&1
done
# 输出示例
# ✅ 端口 80: 开放
# ✅ 端口 443: 开放
# ❌ 端口 8080: 关闭
# ❌ 端口 8443: 关闭
```

## 不适用场景

以下场景网络配置管理（专业版）不适合处理：

- 无明确技术栈的模糊需求
- 纯架构设计决策
- 物理网络布线管理

## 触发条件

需要网络诊断、连通性检测、端口扫描、DNS排查时使用。不适用于非本工具能力范围的需求.

## 快速开始

1. 阅读## 核心能力章节了解skill功能
2. 按## 依赖说明配置环境
3. 执行所需能力对应的命令
4. 参考## 错误处理章节处理异常
5. 查看## FAQ解答常见疑问

```bash
# 1. 连通性检测
ping -c 4 target.host
# 2. 端口扫描
nc -zv target.host 443
# 3. DNS诊断
dig target.host
# 4. 路由追踪
traceroute target.host
# 5. 网卡状态
ifconfig -a
```

**响应解析**: 完成完成后,查看输出响应确认任务状态。成功时输出包含解析摘要和响应数据;失败时根据错误信息排查问题,查阅错误解析章节获取恢复步骤.

## 配置示例

```bash
# 网络诊断配置
TARGET_HOST="api.example.com"
CHECK_PORTS="80 443 8080"
DNS_SERVER="8.8.8.8"
# 命令说明
| 命令    | 说明                      |
|:-----|:-----|
| ping   | 连通性检测                 |
| scan   | 端口扫描                  |
| dns    | DNS解析检查               |
| trace  | 路由追踪                  |
| status | 网卡状态                  |
```

## 最佳实践

* 排查网络问题时先用 `ping` 检测基本连通性.
* 端口不通时用 `traceroute` 定位中断节点.
* DNS异常时对比不同DNS服务器的解析结果.
* 端口扫描建议设置超时参数,避免长时间等待.
* 定期检测关键服务的端口可达性,确保服务正常.
* 网络配置变更后立即执行连通性验证.

## 常见问题

**Q：连通性检测显示丢包怎么办？**
A：使用traceroute定位丢包发生的节点，检查该节点的网络配置和带宽状况.

**Q：DNS解析正常但无法连接？**
A：DNS解析只是第一步，还需用nc/telnet检测目标端口是否开放，可能是防火墙拦截.

**Q：端口扫描被拦截？**
A：部分云服务器有安全组规则，需在云控制台检查安全组配置，开放相应端口.

**Q：支持哪些操作系统？**
A：支持 Windows、macOS、Linux。部分命令因系统而异(macOS用traceroute，Linux用tracepath).

**Q：检测超时怎么处理？**
A：增加超时参数 `-w 5`，或检查本地网络是否正常.

## 依赖说明

### 运行环境
- **Agent平台**: 支持SKILL.md的任意AI Agent(Claude Code / Cursor / Codex / Gemini CLI等)
- **操作系统**: Windows / macOS / Linux
- **网络工具**: ping/nc/dig/traceroute（系统自带或通过包管理器安装）

### 依赖详情
| 依赖项 | 类型 | 是否必需 | 获取方式 |
|---:|---:|---:|---:|
| LLM API | API | 必需 | 由Agent内置LLM提供 |
| ping/nc/dig | CLI工具 | 必需 | 系统自带或包管理器安装 |
| bash | 运行时 | 必需 | 系统自带 |

### API Key 配置
- 本skill基于Markdown指令规范，无需额外API Key
- 网络诊断命令在本地执行，无需外部API

### 可用性分类
- **分类**: MD+EXEC（Markdown指令 + Shell脚本执行）
- **说明**: 基于Markdown的AI Skill，通过 Shell 脚本执行网络诊断命令

## 错误处理

| 错误场景 | 原因 | 处理方式 |
|:---:|:---:|:---:|
| 配置错误 | 参数缺失或格式错误 | 检查依赖说明中的配置要求 |
| 运行时错误 | 运行环境不满足 | 确认运行环境符合依赖说明 |
| 网络错误 | 连接超时或不可达 | 执行ping命令测试网络连通性,检查防火墙和安全组设置 |

## 已知限制

- 本地运行，不支持远程诊断
- 专业版提供完整功能,含批量扫描和报告生成

## 示例

### 基本用法

**输出**：返回执行结果,包含操作状态和输出数据

```text
用户: 检测 api.example.com 的连通性
Skill: 正在执行连通性检测...
Skill: 执行完成,结果如下: 连通正常,平均延迟45ms
```

## 输出格式
```json
{
  "success": true,
  "data": {
    "result": "网络配置管理工具(专业版)处理结果",
    "execution_time": "0.5s",
    "metadata": {
      "version": "1.1",
      "processor": "netdiag"
    }
  },
  "execution_log": ["解析输入参数", "执行核心处理", "格式化输出结果"],
  "error": null
}
```
