---
name: "anygen-diagram-tool-free"
description: "通过自然语言生成图表与可视化结构,支持流程图、架构图等,适合个人用户快速出图。Use when 需要数据分析、报表生成、统计洞察、数据可视化时使用。不适用于实时流数据处理。适用于独立开发者、企业团队和自动化工作流场景。支持中文交互，无需复杂配置即开即用。输出结果可直接使用，减少二次加工成本。提供结构化输出和错误处理机制。"
license: Proprietary
allowed-tools: read exec
compatibility: "Requires LLM with tool-use capability"
metadata:
  displayName: "AnyGen图表生成-免费版"
  version: "1.0.0"
  summary: "通过自然语言生成图表与可视化结构,支持流程图、架构图等,适合个人用户快速出图。"
  tags:
    - "Creative"
    - "图表生成"
    - "可视化"
  source: "SkillHub"
  converted_at: "2026-07-22T17:58:36"
tools:
  - exec
  - read
  - write
  - browser
---
# AnyGen图表生成工具 - 免费版

## 概述

AnyGen图表生成免费版是一款面向个人用户的智能图表与可视化结构生成工具。通过 AnyGen CLI 调用服务端渲染能力,用自然语言描述即可生成流程图、架构图、组织结构图等多种图表,无需手动绘制,几秒钟即可获得高质量图像。

本版本适合个人文档作者、学生及知识工作者,快速将文字描述转化为可视化图表,提升内容表达效果。

## 核心能力

| 能力项 | 免费版支持 | 说明 |
|:-------|:-----------|:-----|
| 自然语言生成图表 | 是 | 描述即出图 |
| 流程图 | 是 | 流程与步骤 |
| 架构图 | 是 | 系统架构 |
| 组织结构图 | 是 | 层级关系 |
| 服务端渲染 | 是 | 高质量输出 |
| 简单认证 | 是 | 浏览器授权 |
| 批量生成 | 否 | PRO 版支持 |
| 自定义模板 | 否 | PRO 版支持 |
| 团队协作 | 否 | PRO 版支持 |
| API 集成 | 否 | PRO 版支持 |
**技术实现要点**：核心能力基于`input_params`参数与`output_format`配置实现,支持创建/查询/修改/删除等操作模式,通过`config_options`进行运行时配置。

### 核心功能执行
用`input_params`参数进行配置。

**输出**: 返回核心功能执行的执行结果,包含操作状态和输出数据。
- 执行此能力时使用`input_params`参数,支持创建/查询/导出操作

### 参数配置与调用
用`config_options`参数进行配置。

**输出**: 返回参数配置与调用的执行结果,包含操作状态和输出数据。
- 执行此能力时使用`config_options`参数,支持修改/重置/导入操作

### 结果处理与输出
用`output_format`参数进行配置。

**输出**: 返回结果处理与输出的执行结果,包含操作状态和输出数据。
- 执行此能力时使用`output_format`参数,支持导出/保存/转换操作
**能力覆盖范围**：本skill的核心能力覆盖以下场景关键词：通过自然语言生成、图表与可视化结构、支持流程图、架构图等、适合个人用户快速、AnyGen、图表生成免费版、面向个人用户的智、能图表与可视化结、构生成工具等。这些关键词对应description中声明的使用场景,均已在上述能力点中提供对应的操作支持。

## 使用场景

### 场景一:文档配图生成

用户在编写技术文档时,需要一张系统架构图说明组件关系。

```bash
# 认证(首次使用)
anygen auth login --no-wait

# 生成架构图
anygen smart_draw "一个微服务架构图:前端通过API网关访问3个微服务(用户服务、订单服务、支付服务),每个服务连接独立的数据库,消息队列连接订单服务与支付服务"
```

### 场景二:学习笔记可视化

学生希望将一个算法流程转化为流程图,便于理解与复习。

```bash
# 生成算法流程图
anygen smart_draw "快速排序算法流程图:开始→选择基准值→分区(小于基准/大于基准)→递归排序→合并结果→结束"
```

### 场景三:项目流程说明

项目经理需要一张项目流程图说明开发流程。

```bash
# 生成项目流程图
anygen smart_draw "软件开发流程图:需求分析→设计→编码→测试→部署→维护,测试失败回到编码,维护完成后结束"
```

## 不适用场景

以下场景AnyGen图表生成-免费版不适合处理：

- 逆向工程闭源API
- API安全渗透测试
- 非标准协议集成

## 触发条件

需要API集成、接口对接、Webhook配置、系统连接时使用。不适用于非本工具能力范围的需求。

## 快速开始

### 领先步:认证

```bash
# 浏览器授权(推荐)
anygen auth login --no-wait

# 或使用 API Key
anygen auth login --api-key sk-未指定

# 或通过环境变量
export ANYGEN_API_KEY=sk-未指定
```

当命令返回认证错误时,运行 `anygen auth login --no-wait` 并在浏览器完成授权,登录成功后重试。

### 依赖详情

```bash
# 安装 anygen-workflow-generate 技能
anygen skill install --platform <skill-platform|claude-code> -y
```

### 第三步:生成图表

```bash
# 自然语言描述生成
anygen smart_draw "你的图表描述"
```

## 示例

基础配置项说明:

```bash
# 认证方式
# 方式1:浏览器授权
anygen auth login --no-wait

# 方式2:API Key
anygen auth login --api-key sk-未指定

# 方式3:环境变量
export ANYGEN_API_KEY=sk-未指定

# 生成命令
anygen smart_draw "<图表描述>"

# 支持的图表类型(通过描述自动识别)
# - 流程图:描述步骤与流程
# - 架构图:描述系统组件与关系
# - 组织结构图:描述层级与归属
# - 思维导图:描述主题与分支
# - 时序图:描述交互时序
```

## 优选实践

1. **描述要具体**:明确节点名称与关系,如"用户服务连接用户数据库"而非"服务连数据库"
2. **关系方向清晰**:用"访问"、"连接"、"调用"等明确关系动词
3. **层级结构分明**:描述时按层级组织,先总后分,便于理解结构
4. **认证失败重试**:遇到认证错误先运行 `anygen auth login --no-wait`,浏览器完成授权后重试
5. **复杂图分步生成**:过于复杂的图可分多张生成,避免单图信息过载
6. **结果保存**:生成的图表会返回 URL 或文件路径,及时保存到本地

## 常见问题

### Q1:认证失败怎么办?
A:运行 `anygen auth login --no-wait`,在浏览器完成授权后重试。也可使用 `--api-key` 方式或设置 `ANYGEN_API_KEY` 环境变量。

### Q2:支持哪些图表类型?
A:支持流程图、架构图、组织结构图、思维导图、时序图等多种类型,通过自然语言描述自动识别。

### Q3:生成的图表是什么格式?
A:服务端渲染输出高质量图像,通常为 PNG 或 SVG 格式。具体格式取决于 AnyGen 服务返回。

### 已知限制
A:免费版有调用次数限制,适合个人日常使用。如需批量生成或团队协作,请使用 PRO 版。

### Q5:需要安装工作流技能吗?
A:首次使用需安装 `anygen-workflow-generate` 技能,通过 `anygen skill install` 命令安装。已安装则可直接使用。

### Q6:可以生成中文图表吗?
A:可以。自然语言描述支持中文,生成的图表中的文字也会使用中文。
- 当前为免费版本,如需完整功能请升级到付费版获取全部能力
## 依赖说明

### 运行环境
- **Agent平台**: 支持 SKILL.md 的任意 AI Agent(Claude Code / Cursor / Codex / Gemini CLI 等)
- **操作系统**: Windows / macOS / Linux
- **运行时**: AnyGen CLI

### 第三方依赖
| 依赖项 | 类型 | 是否必需 | 获取方式 |
|:-------|:-----|:---------|:---------|
| AnyGen CLI | 命令行工具 | 必需 | 官方安装 |
| AnyGen API | 外部 API | 必需 | 注册获取 API Key |
| anygen-workflow-generate | 工作流技能 | 必需 | anygen skill install |
| 浏览器 | 认证 | 推荐 | 用于浏览器授权 |

### API Key 配置
- **环境变量名**: `ANYGEN_API_KEY`
- **获取方式**: AnyGen 平台注册,或通过浏览器授权
- **配置方式**: 环境变量、CLI 登录(`anygen auth login --api-key`)、浏览器授权
- **安全建议**: API Key 妥善保管,泄露后及时在平台轮换

### 可用性分类
- **分类**: MD+execute(纯 Markdown 指令,核心功能需要 exec 命令行执行能力)
- **说明**: 基于Markdown的AI Skill,通过AnyGen CLI调用服务端渲染生成图表

## 错误处理

| 错误场景 | 原因 | 处理方式 |
|---------|------|---------|
| 配置错误 | 参数缺失或格式错误 | 检查依赖说明中的配置要求 |
| 运行时错误 | 运行环境不满足 | 确认运行环境符合依赖说明 |
| 网络错误 | 连接超时或不可达 | 执行ping命令测试网络连通性,检查防火墙和代理设置连接后执行ping命令测试网络连通性,检查防火墙和代理设置连接后重新执行命令，参考国内替代方案 |

## 安全注意事项

| 风险类型 | 防范措施 |
|----------|---------|
| API密钥泄露 | 通过环境变量配置，禁止硬编码到代码或配置文件中 |
| 命令执行风险 | 仅执行白名单命令，避免拼接用户输入到命令行参数中 |
| 网络通信安全 | 使用HTTPS协议，验证SSL证书有效性 |
| 敏感数据暴露 | 输出结果中不包含密钥、令牌等敏感信息 |

使用前请确认已阅读依赖说明章节，确保运行环境满足安全要求。

## 效率量化分析

| 操作场景 | 手动耗时 | 自动化耗时 | 效率提升 |
|----------|---------|-----------|---------|
| 文件解析与提取 | 5-10分钟/个 | <5秒/个 | 60-120x |
| 批量文件处理(100个) | 8-16小时 | <5分钟 | 96-192x |
| API调用与响应解析 | 2-3分钟/次 | <1秒/次 | 120-180x |
| 多接口数据聚合 | 15-30分钟 | <10秒 | 90-180x |
| 命令执行与结果收集 | 3-5分钟/次 | <2秒/次 | 90-150x |
| 重复任务批量执行 | 因任务而异 | 线性缩减 | 5-50x |
| 错误排查与修复 | 10-30分钟 | <30秒 | 20-60x |

## 差异化对比

| 对比维度 | 本技能 | 传统手动方式 | 通用脚本工具 |
|---------|------------|-------------|------------|
| 自动化程度 | 全流程自动 | 完全手动 | 部分自动 |
| 错误处理 | 内置错误恢复 | 依赖人工经验 | 基本try-catch |
| 可复用性 | 参数化配置 | 一次性脚本 | 模板化 |
| 安全合规 | 内置安全检查 | 无安全保障 | 无安全保障 |
| 适用场景 | 核心功能 | 通用场景 | 通用场景 |